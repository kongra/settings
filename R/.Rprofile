options(warn              = 1L,
        prompt            = "R> ",
        digits            = 7,
        show.signif.stars = FALSE,
        browser           = "/usr/bin/firefox",

        show.error.locations = TRUE,
        show.error.messages  = TRUE,

        shiny.port           = 8080,
        shiny.launch.browser = FALSE,
        shiny.fullstacktrace = TRUE)

Sys.setenv(LANGUAGE = "en")

# Comment the one below to install some packages (e.g. quanteda):
Sys.setlocale(locale = "en_US.utf8") # R> system("locale -a", intern = TRUE)

# Sys.setlocale("LC_ALL", "en_US.utf8")
# .libPaths("/home/kongra/R/x86_64-pc-linux-gnu-library/3.5")
# .libPaths("/home/kongra/R/x86_64-pc-linux-gnu-library/3.6")
# .libPaths("/home/kongra/R/x86_64-pc-linux-gnu-library/4.0")
.libPaths("/home/kongra/R/x86_64-pc-linux-gnu-library/4.1")
